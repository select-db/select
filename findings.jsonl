{"id": "f-r1-001", "run": "r1", "layer": "permission", "group_key": "permission|bypass|read_site=set_operation_branch,dialect=sqlite", "severity": "bypass", "oracle": "cross-dialect", "dialects": ["sqlite"], "cases": ["c-r1-014-sqlite", "c-r1-019-sqlite", "c-r1-020-sqlite", "c-r1-021-sqlite", "c-r1-022-sqlite"], "summary": "on SQLite a statement whose source query is a compound SELECT (UNION, EXCEPT, INTERSECT, not UNION ALL) loses its outer statement: the inspector reports a plain select over one branch, so the write is never checked and the other branches are never named", "scenario": "an analyst holding only select on other.t3 writes rows into main.t1 with INSERT INTO t1 (c1) SELECT c1 FROM t2 UNION SELECT c1 FROM other.t3, and with a constant last branch the deny-all policy allows the statement outright, so a principal holding no grant at all inserts rows", "defence": "the merge in dialect/sqlite/inspect.go exists to repair a parser artifact, since ANTLR splits A UNION B into adjacent sql_stmt_list nodes and without it even SELECT 1 UNION SELECT 2 reports as two statements, so this could be read as the known unreadable-branch-degrades-to-nothing class scoped to one dialect. It does not hold: the insert_stmt branch parses cleanly and is then discarded, so the result is not an empty read but a write relabelled as a read, which defeats the UnknownStatement floor by construction; PostgreSQL, MySQL and SQLite with UNION ALL all get it right", "status": "filed", "issue": 304}
{"id": "f-r1-002", "run": "r1", "layer": "permission", "group_key": "permission|wrong-right|operation=insert,read_site=cte,dialect=mysql", "severity": "wrong-right", "oracle": "cross-dialect", "dialects": ["mysql"], "cases": ["c-r1-001-mysql", "c-r1-002-mysql"], "summary": "WITH before INSERT on the MySQL dialect floors to unknown statements and asks for manage, so no right on the insert target is ever checked", "scenario": "no realistic one: MySQL rejects WITH ... INSERT with a syntax error, so no statement runs and no row is touched", "defence": "MySQL permits WITH at the head of SELECT, UPDATE and DELETE and immediately before the SELECT of an INSERT ... SELECT, and nowhere else; MySQLParser.g4 carries withClause on deleteStatement, queryExpression and updateStatement but not on insertStatement. The cases are the one spelling the server refuses, so the unknown-statement floor is the designed answer and the refusal costs a data role nothing. The defence holds; the cases were rewritten as skips and the expectation was wrong. The residue is a diagnosis quality defect at most: the user is told they lack manage when the truth is a syntax error", "status": "defended"}
{"id": "f-r1-003", "run": "r1", "layer": "permission", "group_key": "permission|wrong-right|operation=merge,dialect=postgresql", "severity": "wrong-right", "oracle": "judgment", "dialects": ["postgresql"], "cases": ["c-r1-008-postgresql"], "summary": "PostgreSQL MERGE floors to an unknown statement and asks only for manage, so neither the update on the target nor the reads of the source are checked", "scenario": "an operator holding manage but no insert or update right runs MERGE INTO t1 USING t2 ... WHEN MATCHED THEN UPDATE SET c2 = b.c3 and rewrites rows of main.t1, while an analyst holding the four data actions is refused a statement they should be allowed", "defence": "not reviewed adversarially this run; the unknown-statement floor is documented behaviour for a statement the inspector does not dispatch on, and MERGE may simply be unsupported on purpose", "status": "suspected"}
{"id": "f-r2-001", "run": "r2", "layer": "lint", "group_key": "lint|quality|dialect=mysql,operation=multi_delete;multi_update,naming=aliased", "severity": "quality", "oracle": "judgment", "dialects": ["mysql"], "cases": ["c-r2-lint-001", "c-r2-lint-002", "c-r2-lint-003"], "summary": "the alias of a target in a MySQL multi-table DELETE list is linted as an unknown table, and the alias of the second table of a multi-table UPDATE list does not resolve its columns, so valid MySQL is marked with unknown-table and unknown-column warnings", "scenario": "a MySQL user writes the ordinary join-delete DELETE a FROM t1 AS a JOIN t2 ON a.c1 = t2.c1 or the join-update UPDATE t1 AS a, t2 AS b SET a.c2 = 'x' WHERE a.c1 = b.c1; both are valid and both get the right permission verdict, but the editor warns unknown table or view 'a' and unknown column 'c1', so the user either learns to ignore the two rules most worth trusting or rewrites correct SQL to silence them", "defence": "the authoritative permission layer is right in all six cases, so nothing is over-granted or under-granted and what remains is a severity warning marker that blocks nothing; analyze_unknown_tables in lint_rules/references.py:32 walks every exp.Table, sqlglot's MySQL parse puts the pre-FROM target there, the rule has no alias-binding context at that point, and it does catch the real typo DELETE aa FROM t1 AS a. It does not hold: a warning that fires on valid SQL is the false positive class that teaches users to ignore the rule, and DELETE t1 FROM t1 JOIN t2 lints clean only because a table named t1 exists, so the rule is not distinguishing an alias from a table, it is looking the name up in the catalog. The review is right that there are two code sites, so the fix is two changes: alias scope for the DELETE target list, and a sources that covers the whole MySQL table reference list rather than args[from_] alone", "status": "filed", "issue": 314}
{"id": "f-r1-001", "run": "36006015472", "layer": "permission", "group_key": "permission|bypass|read_site=set_operation_branch,dialect=sqlite", "severity": "bypass", "oracle": "cross-dialect", "dialects": ["sqlite"], "cases": ["c-r1-014-sqlite", "c-r1-019-sqlite", "c-r1-020-sqlite", "c-r1-021-sqlite", "c-r1-022-sqlite"], "summary": "on SQLite a statement whose source query is a compound SELECT (UNION, EXCEPT, INTERSECT, not UNION ALL) loses its outer statement: the inspector reports a plain select over one branch, so the write is never checked and the other branches are never named", "scenario": "an analyst holding only select on other.t3 writes rows into main.t1 with INSERT INTO t1 (c1) SELECT c1 FROM t2 UNION SELECT c1 FROM other.t3, and with a constant last branch the deny-all policy allows the statement outright, so a principal holding no grant at all inserts rows", "defence": "the merge in dialect/sqlite/inspect.go exists to repair a parser artifact, since ANTLR splits A UNION B into adjacent sql_stmt_list nodes and without it even SELECT 1 UNION SELECT 2 reports as two statements, so this could be read as the known unreadable-branch-degrades-to-nothing class scoped to one dialect. It does not hold: the insert_stmt branch parses cleanly and is then discarded, so the result is not an empty read but a write relabelled as a read, which defeats the UnknownStatement floor by construction; PostgreSQL, MySQL and SQLite with UNION ALL all get it right", "status": "fixed", "issue": 304, "verdict_by": "Arthur-Mesplede"}
{"id": "f-r2-001", "run": "36006015472", "layer": "lint", "group_key": "lint|quality|dialect=mysql,operation=multi_delete;multi_update,naming=aliased", "severity": "quality", "oracle": "judgment", "dialects": ["mysql"], "cases": ["c-r2-lint-001", "c-r2-lint-002", "c-r2-lint-003"], "summary": "the alias of a target in a MySQL multi-table DELETE list is linted as an unknown table, and the alias of the second table of a multi-table UPDATE list does not resolve its columns, so valid MySQL is marked with unknown-table and unknown-column warnings", "scenario": "a MySQL user writes the ordinary join-delete DELETE a FROM t1 AS a JOIN t2 ON a.c1 = t2.c1 or the join-update UPDATE t1 AS a, t2 AS b SET a.c2 = 'x' WHERE a.c1 = b.c1; both are valid and both get the right permission verdict, but the editor warns unknown table or view 'a' and unknown column 'c1', so the user either learns to ignore the two rules most worth trusting or rewrites correct SQL to silence them", "defence": "the authoritative permission layer is right in all six cases, so nothing is over-granted or under-granted and what remains is a severity warning marker that blocks nothing; analyze_unknown_tables in lint_rules/references.py:32 walks every exp.Table, sqlglot's MySQL parse puts the pre-FROM target there, the rule has no alias-binding context at that point, and it does catch the real typo DELETE aa FROM t1 AS a. It does not hold: a warning that fires on valid SQL is the false positive class that teaches users to ignore the rule, and DELETE t1 FROM t1 JOIN t2 lints clean only because a table named t1 exists, so the rule is not distinguishing an alias from a table, it is looking the name up in the catalog. The review is right that there are two code sites, so the fix is two changes: alias scope for the DELETE target list, and a sources that covers the whole MySQL table reference list rather than args[from_] alone", "status": "fixed", "issue": 314, "verdict_by": "Arthur-Mesplede"}
{"id": "f-r3-001", "run": "r3", "layer": "permission", "group_key": "permission|unchecked-read|operation=set;call", "severity": "unchecked-read", "oracle": "judgment", "dialects": ["mysql", "postgresql"], "cases": ["c-r3-003-mysql", "c-r3-005-postgresql", "c-r3-005-mysql"], "summary": "a statement the inspectors do not dispatch on reports no tables for the subqueries nested in it, so MySQL SET @v = (SELECT ...) and CALL p((SELECT ...)) on PostgreSQL and MySQL ask for manage alone and no select right is checked for the tables they read", "scenario": "an operator holding manage on the connection and no row right reads a column value of main.t2 into a MySQL user variable with SET @v = (SELECT c1 FROM main.t2 WHERE c3 = 1), then reads the variable back with a statement that names no table; the same operator reads main.t1 through a procedure argument with CALL. A pinned case denies this principal a plain SELECT c1 FROM t1, so the model does not treat manage as a right to read rows", "defence": "operationToAction refuses to admit an operation by silence, so an unclassified statement falls to ActionManage by design, and manage is held by principals who can DROP the table or wrap it in a view and so could reach the rows another way, which would make the dropped subquery a completeness wart rather than a permission hole. It does not hold: the independence of manage and the row rights is a settled rule pinned in both directions in dialect/core/testutil/perm_data.go (manage is denied a plain SELECT c1 FROM t1; the four row rights together are denied a DROP), and the comment on checkStatement in dialect/core/permissions.go says manage never stands in for the select the source still needs. PostgreSQL COPY (SELECT ...) TO STDOUT is the proof the two concerns come apart: same op=unknown, same manage, and it still demands select on the tables its subqueries read", "status": "filed", "issue": 319}
{"id": "f-r1-003", "run": "r3", "layer": "permission", "group_key": "permission|wrong-right|operation=merge,dialect=postgresql", "severity": "wrong-right", "oracle": "judgment", "dialects": ["postgresql"], "cases": ["c-r1-008-postgresql", "c-r3-006-postgresql"], "summary": "PostgreSQL MERGE floors to an unknown statement and asks only for manage, so neither the update on the target nor the reads of the source are checked", "scenario": "an operator holding manage but no update or select right runs MERGE INTO t1 USING t2 ON t1.c1 = t2.c1 WHEN MATCHED THEN UPDATE SET c2 = t2.c3 and rewrites rows of main.t1 while reading main.t2, and an analyst holding the four data actions is refused a statement they are entitled to run; the completion layer offers MERGE as an opener in dialect/postgresql/dialect.go", "defence": "the doc comment on core.checkInstance says manage is the only check a statement the package could not resolve can get, because that statement names no table and a per-table walk would see nothing; MERGE is in no statement switch of the PostgreSQL inspector, so the floor behaves as documented, and the only user-visible harm would be a false denial of an unsupported statement. It does not hold: the floor is fail-closed only if manage dominates the row rights, and method.md lists their independence among the settled rules, pinned by the two cases in dialect/core/testutil/perm_data.go. The manage-only principal, whom a pinned case denies even a SELECT, rewrites main.t1 here, so this is the wrong right rather than a false denial, and it ranks above one", "status": "filed", "issue": 320}
{"id": "f-r3-002", "run": "r3", "layer": "permission", "group_key": "permission|false-denial|operation=copy,dialect=postgresql", "severity": "false-denial", "oracle": "judgment", "dialects": ["postgresql"], "cases": ["c-r3-001-postgresql", "c-r3-002-postgresql"], "summary": "PostgreSQL COPY (query) TO STDOUT reports op=unknown and so asks for manage on top of the rights its query needs, and the data policy refuses it although PostgreSQL itself asks only for the rights on the tables the query touches", "scenario": "an analyst holding select, insert, update and delete on every table and no manage exports rows with COPY (SELECT c1 FROM t1 LIMIT (SELECT c1 FROM t2)) TO STDOUT, which the server would allow on their select rights alone, and the package refuses it for want of manage; the same happens to COPY (INSERT ... RETURNING ...) TO STDOUT", "defence": "not reviewed adversarially this run: COPY does report the subqueries nested in it, so the reads and the write are checked, and requiring manage on top is fail-closed, which is the documented answer for a statement the inspector does not dispatch on. Held for a run with time to review it", "status": "suspected"}
{"id": "f-r3-001", "run": "36037095556", "layer": "permission", "group_key": "permission|unchecked-read|operation=set;call", "severity": "unchecked-read", "oracle": "judgment", "dialects": ["mysql", "postgresql"], "cases": ["c-r3-003-mysql", "c-r3-005-postgresql", "c-r3-005-mysql"], "summary": "a statement the inspectors do not dispatch on reports no tables for the subqueries nested in it, so MySQL SET @v = (SELECT ...) and CALL p((SELECT ...)) on PostgreSQL and MySQL ask for manage alone and no select right is checked for the tables they read", "scenario": "an operator holding manage on the connection and no row right reads a column value of main.t2 into a MySQL user variable with SET @v = (SELECT c1 FROM main.t2 WHERE c3 = 1), then reads the variable back with a statement that names no table; the same operator reads main.t1 through a procedure argument with CALL. A pinned case denies this principal a plain SELECT c1 FROM t1, so the model does not treat manage as a right to read rows", "defence": "operationToAction refuses to admit an operation by silence, so an unclassified statement falls to ActionManage by design, and manage is held by principals who can DROP the table or wrap it in a view and so could reach the rows another way, which would make the dropped subquery a completeness wart rather than a permission hole. It does not hold: the independence of manage and the row rights is a settled rule pinned in both directions in dialect/core/testutil/perm_data.go (manage is denied a plain SELECT c1 FROM t1; the four row rights together are denied a DROP), and the comment on checkStatement in dialect/core/permissions.go says manage never stands in for the select the source still needs. PostgreSQL COPY (SELECT ...) TO STDOUT is the proof the two concerns come apart: same op=unknown, same manage, and it still demands select on the tables its subqueries read", "status": "fixed", "issue": 319, "verdict_by": "Arthur-Mesplede"}
