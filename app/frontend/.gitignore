node_modules

/.svelte-kit
/build
/node_modules

.DS_Store
Thumbs.db

.env
.env.*
!.env.example
!.env.test

# Playwright (e2e run output)
/test-results
/playwright-report

# Local browser pin: points Playwright at a Chromium already on this machine,
# so the path is only ever right on the machine that wrote it.
/playwright.pinned.config.ts
